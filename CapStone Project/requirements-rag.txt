# Optional embedding-retrieval stack for RAG evidence search.
# Install this on top of the base profile when you want semantic retrieval.
-r requirements.txt

numpy>=1.26.0
sentence-transformers>=3.0.0
# FAISS has no consistently available Windows wheel. ClaimGuard uses cosine
# similarity when it is absent and FAISS automatically on supported platforms.
faiss-cpu>=1.8.0; platform_system != "Windows"

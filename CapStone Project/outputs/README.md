# Output index

- `evaluation/`: all metrics cited by the report, plus an artifact-to-command map.
- `examples/`: saved RQ5 OpenAI/Ollama analyses used by the presentation.
- `models/scifact-lora/`: the single selected deployable LoRA adapter and training provenance.

Discarded model trials, optimizer checkpoints, incomplete baselines, and superseded demo outputs
were removed during the final repository cleanup.
